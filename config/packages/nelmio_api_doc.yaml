nelmio_api_doc:
    documentation:
        info:
            title: Vilgain Task API
            description: REST API for User and Article management
            version: 1.0.0
        components:
            securitySchemes:
                Bearer:
                    type: http
                    scheme: bearer
                    bearerFormat: JWT
        security:
            - Bearer: []
    areas:
        path_patterns:
            - ^/(?!api/doc|_)
